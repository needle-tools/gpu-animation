#pragma kernel CSMain

Texture2D<float> IdMap;
SamplerState samplerIdMap;

float2 SampleUV;
RWStructuredBuffer<int> Result;

[numthreads(1,1,1)]
void CSMain(uint3 id : SV_DispatchThreadID)
{
    float val = IdMap.SampleLevel(samplerIdMap, SampleUV, 0);
    Result[0] = (int)val;
}
